# Optional public port. Cloudflare Tunnel should point to this port.
PORT=8080
# SQLite file location. Leave unset to use wearsic.db in the server directory.
WEARSIC_DB_PATH=wearsic.db
# Optional shared key. If set, the watch app must use the same key in Settings.
WEARSIC_API_KEY=
